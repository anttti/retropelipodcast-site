---
layout: layouts/base.njk
---

<main class="relative">
  <div class="relative z-40 pt-40">
    <section
      class="latest-episode mb-52 mx-auto p-4 space-y-4 max-w-xl relative z-50"
    >
      <div class="decoration"></div>
      <img
        class="absolute"
        style="top: -148px; left: 0"
        src="/img/duosaurus.png"
        alt="Two dinos"
      />
      <img
        class="absolute"
        style="top: -165px; left: 200px"
        src="/img/bluesaurus.png"
        alt="One blue dino"
      />
      <h3 class="font-bold">{{ media.episode }}. {{ media.title | safe }}</h3>
      <time>{{ media.publishDate | readableDate }}</time>
      <section class="prose">
        {{ content | safe }}
      </section>
      <audio controls="controls" src="{{ media.content }}">
        Selaimesi ei tue audio-elementtiä.
      </audio>
    </section>

    <div class="relative z-10">
      <div class="bg absolute w-full">
        <div
          class="bg-1 h-full bg-bottom bg-contain bg-repeat-x w-full absolute z-10"
        ></div>
        <div
          class="bg-2 h-full bg-bottom bg-contain bg-repeat-x w-full absolute z-20"
        ></div>
        <div
          class="bg-3 h-full bg-bottom bg-contain bg-repeat-x w-full absolute z-30"
        ></div>
      </div>

      <div class="ground relative z-40 w-full"></div>

      <section class="episodes relative z-40 px-12 py-8"></section>
    </div>
  </div>
</main>
